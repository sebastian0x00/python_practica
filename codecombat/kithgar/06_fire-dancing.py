# El código se suele ejecutar en el orden escrito
# los búcles te permiten repetir un bloque de código repetidas veces
# Necesitar presionar tabulat (o poner 4 espacios) para tabular los movimientos bajo el búcle

while True:
    hero.moveRight()
    # estos comentario abajo muestran cómo hacer un bucle
    hero.moveLeft()
